import { StyleSheet } from 'react-native';

const base = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  list: {
    height: 68,
    borderBottomColor: '#2e2e2e',
    borderBottomWidth: 1
  }
});

export default base;
